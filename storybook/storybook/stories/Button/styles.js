import { Button } from 'builders-ui';
import styled from 'styled-component/native';

export const StyledButton = styled(Button).attrs({ secondary: true })`
  width: 232px;
  margin-top: 45px;
  align-self: center;
`;
